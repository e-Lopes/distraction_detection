experiment_name: "g4_smoke_b"
generation: "G4SMOKE"
configuration_id: "g4_smoke_b"
scenario: "B"
representation: "R0"
window_size_frames: 60
preprocessing_config: "fase_2/configs/preprocessing/baseline_zero_fill.yaml"
models: [tcn]
seeds: [42]
max_samples_per_subset: 64
training: {max_epochs: 2, batch_size: 32, learning_rate: 0.001, weight_decay: 0.0001, monitor: "val_macro_f1", mode: "max", patience: 1, minimum_delta: 0.002, checkpoint_every_epochs: 1, gradient_clip_norm: 1.0, amp: true, deterministic: true, num_workers: 0, balancing: "class_weights"}
parameters:
  tcn: {channels: [32, 64, 64], kernel_size: 3, dropout: 0.25, pooling: "mean"}
